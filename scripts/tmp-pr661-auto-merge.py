from pathlib import Path

def replace_once(path, old, new):
    target = Path(path)
    text = target.read_text(encoding='utf-8')
    count = text.count(old)
    if count != 1:
        raise RuntimeError(f'{path}: expected one match, found {count}')
    target.write_text(text.replace(old, new, 1), encoding='utf-8')

replace_once('tests/electron/modelAliasPresentation.test.js', "test('empty or malformed aliases are a reference-preserving no-op', () => {\n  const stats = { periods: { today: period } };\n  for (const input of [undefined, null, [], 'x', {}, { x: 3, ' ': 'x', y: '' }]) {\n    assert.deepEqual(normalizeModelAliases(input), {});\n    assert.strictEqual(projectModelAliasStats(stats, input), stats);\n  }\n});", "test('empty or malformed alias settings still use automatic duplicate matching', () => {\n  const stats = { periods: { today: period } };\n  for (const input of [undefined, null, [], 'x', {}, { x: 3, ' ': 'x', y: '' }]) {\n    assert.deepEqual(normalizeModelAliases(input), {});\n    assert.deepEqual(projectModelAliasStats(stats, input).periods.today.models, {\n      'claude-opus-5': 70,\n      'gpt-5.5-pro': 30\n    });\n  }\n});")
replace_once('tests/electron/modelAliasPresentation.test.js', '  assert.strictEqual(projectModelAliasStats(stats, {}), stats);', "  assert.deepEqual(projectModelAliasStats(stats, {}).periods.today.models, { 'claude-opus-5': 70, 'gpt-5.5-pro': 30 });")
replace_once('tests/electron/modelAliasIntegration.test.js', "  settings.modelAliases = {};\n  assert.strictEqual(project(raw), raw);\n  assert.equal(raw.periods.today.models['anthropic/claude-opus-5'], 20);", "  settings.modelAliases = {};\n  assert.deepEqual(project(raw).periods.today.models, { 'claude-opus-5': 50 });\n  assert.equal(raw.periods.today.models['anthropic/claude-opus-5'], 20);")
replace_once('tests/electron/modelAliasIntegration.test.js', "  settings.modelAliases = {};\n  assert.equal((await getHistory()).daily[0].perModel['anthropic/claude-opus-5'].tokens, 20);", "  settings.modelAliases = {};\n  assert.deepEqual((await getHistory()).daily[0].perModel, {\n    'claude-opus-5': { tokens: 50, cost: 9, unclassifiedTokens: 50 }\n  });")
replace_once('README.md', '**Model aliases** in Collection settings let you merge equivalent model IDs in local views, including retained history, without changing costs or stored/synced/exported data. See the [model alias guide](docs/model-aliases.md).', '**Model aliases** automatically merge observed provider-qualified and separator variants in local views; manual mappings cover names that cannot be matched safely. Costs and stored, synced, or exported IDs remain unchanged. See the [model alias guide](docs/model-aliases.md).')
replace_once('src/electron/renderer/index.html', "Merge model names only in this app's views, including history. Costs, source logs, sync and exports are unchanged. Remove mappings to restore separate groups.", "Equivalent provider-qualified and separator variants are merged automatically in this app's views. Add an alias only when the automatic matcher cannot prove two names are equivalent. Costs, source logs, sync and exports are unchanged.")
replace_once('src/electron/renderer/index.html', 'Matching ignores case and dots versus hyphens, but keeps dated versions and tiers distinct. Mappings apply once; only map names you know are the same model.', 'Automatic matching keeps dated versions, tiers and model variants distinct. Manual mappings apply once and can override a specific reported ID.')
replace_once('src/electron/renderer/i18n.js', '      \'settings.modelAliases.note\': "Merge model names only in this app\'s views, including history. Costs, source logs, sync and exports are unchanged. Remove mappings to restore separate groups.",', '      \'settings.modelAliases.note\': "Equivalent provider-qualified and separator variants are merged automatically in this app\'s views. Add an alias only when the automatic matcher cannot prove two names are equivalent. Costs, source logs, sync and exports are unchanged.",')
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.matching': 'Matching ignores case and dots versus hyphens, but keeps dated versions and tiers distinct. Mappings apply once; only map names you know are the same model.',", "      'settings.modelAliases.matching': 'Automatic matching keeps dated versions, tiers and model variants distinct. Manual mappings apply once and can override a specific reported ID.',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.none': 'Off',", "      'settings.modelAliases.none': 'Automatic',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.note': '僅在本機應用程式的檢視中合併模型名稱，包含歷史資料。費用、原始記錄、同步與匯出皆不變。移除對應即可恢復分開顯示。',", "      'settings.modelAliases.note': '本應用程式會在檢視中自動合併帶 provider 前綴或僅分隔符不同的同一模型名稱。只有自動規則無法確認時才需要新增別名。費用、原始記錄、同步與匯出皆不變。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.matching': '比對不區分大小寫、句點與連字號，但保留日期版本與等級差異。對應僅套用一次；請只合併已確認為同一模型的名稱。',", "      'settings.modelAliases.matching': '自動比對會保留日期版本、等級與模型變體的差異。手動對應僅套用一次，並可覆寫特定的原始模型 ID。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.none': '未啟用',", "      'settings.modelAliases.none': '自動',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.note': '仅在本机应用的视图中合并模型名称，包括历史数据。费用、原始记录、同步和导出均不变。移除映射即可恢复分开显示。',", "      'settings.modelAliases.note': '本应用会在视图中自动合并带 provider 前缀或仅分隔符不同的同一模型名称。只有自动规则无法确认时才需要添加别名。费用、原始记录、同步和导出均不变。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.matching': '匹配不区分大小写、句点和连字符，但保留日期版本与等级差异。映射仅应用一次；请只合并已确认为同一模型的名称。',", "      'settings.modelAliases.matching': '自动匹配会保留日期版本、等级和模型变体的差异。手动映射仅应用一次，并可覆盖某个特定的原始模型 ID。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.none': '未启用',", "      'settings.modelAliases.none': '自动',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.note': '기록을 포함한 이 앱의 화면에서만 모델 이름을 합칩니다. 비용, 원본 로그, 동기화 및 내보내기는 변경되지 않습니다. 매핑을 삭제하면 다시 분리됩니다.',", "      'settings.modelAliases.note': '이 앱은 provider 접두사나 구분자만 다른 동일 모델 이름을 화면에서 자동으로 합칩니다. 자동 규칙으로 확인할 수 없는 경우에만 별칭을 추가하세요. 비용, 원본 로그, 동기화 및 내보내기는 변경되지 않습니다.',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.matching': '대소문자와 점·하이픈은 구분하지 않지만 날짜 버전과 등급은 구분합니다. 매핑은 한 번만 적용됩니다. 같은 모델임을 확인한 이름만 연결하세요.',", "      'settings.modelAliases.matching': '자동 일치는 날짜 버전, 등급 및 모델 변형을 구분합니다. 수동 매핑은 한 번만 적용되며 특정 원본 모델 ID를 재정의할 수 있습니다.',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.none': '꺼짐',", "      'settings.modelAliases.none': '자동',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.note': '履歴を含むこのアプリの表示でのみモデル名を統合します。費用、元のログ、同期、エクスポートは変わりません。対応を削除すると個別の表示に戻ります。',", "      'settings.modelAliases.note': 'このアプリは provider 接頭辞または区切り文字だけが異なる同一モデル名を表示上で自動統合します。自動判定できない場合だけ別名を追加してください。費用、元のログ、同期、エクスポートは変わりません。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.matching': '大文字・小文字とピリオド・ハイフンは区別しませんが、日付付きバージョンと階層は区別します。対応は一度だけ適用されます。同じモデルと確認した名前のみ指定してください。',", "      'settings.modelAliases.matching': '自動照合では日付付きバージョン、階層、モデル派生を区別します。手動対応は一度だけ適用され、特定の報告元モデル ID を上書きできます。',")
replace_once('src/electron/renderer/i18n.js', "      'settings.modelAliases.none': 'オフ',", "      'settings.modelAliases.none': '自動',")
